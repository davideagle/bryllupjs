var invite = require('config/models');

exports.register = function(email,invitecode,callback) {

	invite.find({invite: invitecode},function(err,invites){
		if(invites.length != 0){
			
		} else {
			callback({'response':"Boðskóði ekki til",'res':false});
		}
	});
};